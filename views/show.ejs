<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Post | ShareMind</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <!-- Custom Stylesheet -->
    <link rel="stylesheet" href="/show.css">
</head>
<body>
    <div class="show-card">
        <h2 class="post-user"><i class="bi bi-person-circle"></i> @<%= post.username %></h2>
        <p class="post-content"><%= post.content %></p>
        
        <div class="card-actions">
            <a href="/posts" class="btn btn-secondary"><i class="bi bi-arrow-left-circle-fill"></i> Back to Feed</a>
        </div>

        <div class="post-meta">
            Post ID: <span><%= post.id %></span>
        </div>
    </div>
</body>
</html>
